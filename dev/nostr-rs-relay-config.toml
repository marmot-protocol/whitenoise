[info]
relay_url = "ws://localhost:8080"
name = "whitenoise-dev"
description = "Whitenoise dev nostr relay"

[database]
engine = "sqlite"
data_directory = "/usr/src/app/db"

[network]
address = "0.0.0.0"
port = 8080

[options]
reject_future_seconds = 1800

[limits]
limit_scrapers = false
