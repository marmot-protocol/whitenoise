# OpenCode Kickoff Prompt (Paste As-Is)

Repo: /Users/vladimirkrstic/dev/whitenoise-work/repo/whitenoise
Branch: codex/ui_lab (push target: origin/ui_lab)
Goal: UI inspection + vibecoding only.

Rules:
- Do not add new app features unless explicitly asked.
- Keep changes focused to requested screen/component.
- Run relevant checks before commit.
- No force-push.

Execution flow:
1. Run `git status -sb` and summarize current state.
2. Implement requested UI change.
3. Run targeted validation.
4. Commit with a clear message.
5. Push with `git push origin HEAD:ui_lab`.
6. Return concise summary: what changed, files, checks, caveats.
